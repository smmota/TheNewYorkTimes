name: Build & PR

on:
  push:
    branches:
      - 'feature**'  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Verificar código 
      - uses: actions/checkout@v2

      - name: Restaurar pacotes
        run: dotnet restore

      - name: Build
        run: dotnet build

  #open-pr-to-develop:
    #needs:
      #- build
    #runs-on: gh pr create
    #with:
      #target-branch: 'develop'
    

